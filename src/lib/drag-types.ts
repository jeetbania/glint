/** Shared HTML5 drag-and-drop MIME type for dragging a saved item (from
 * the Library grid, or anywhere else ItemCard is used) onto a folder
 * tile to file it into that collection — see item-card.tsx (drag
 * source) and collections-row.tsx's FolderTile (drop target). */
export const GLINT_ITEM_DRAG_TYPE = "application/x-glint-item-id";
